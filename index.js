require('math/index.js')

/* let namse = "max";
let age = 28;
let pregunta = true;

const evaluar = (variable) => {
  return `Hello` + variable;
};

console.log(evaluar("numero de phi"));
console.log(age, pregunta, namse);

 */
const numeros = [1, 2, 3, 4, 5];

for (let i = 0; i < numeros.length; i++) {
  console.log(numeros[i]);
}




/** 
 *!Esta parte me permite que ver dentro de nodejs, se puede hacer todo lo que javascript permite. 

*/